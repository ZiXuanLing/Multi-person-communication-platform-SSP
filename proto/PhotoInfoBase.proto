syntax = "proto2";
package ssp;
message PhotoInfoBase {
    optional int32 ver=1;
    optional int32 user_id=2;
    optional int32 last_publisher=3;    // The user who refreshed last time
    optional int32 last_publish_time=4; // Time of last refresh
    optional int32 last_publish_message_id=5;
}